Retrieval uses dense embeddings generated locally to keep costs low and latency predictable. Pinecone stores vectors with chunk-level metadata, including title and source path.

Synthesis favors short bullet points with citations. If a question lacks support, the assistant explicitly states what evidence is missing.
